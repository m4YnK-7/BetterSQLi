# Automated SQLi Wrapper (local, file-based)

This project is a lightweight wrapper/orchestrator around `sqlmap` that captures run artifacts
to local files and provides a local Streamlit UI for exploring results.

## Requirements
- Kali (or Linux) with Python 3.9+
- sqlmap installed and in PATH
- pip packages: requests, streamlit

Install Python dependencies:
